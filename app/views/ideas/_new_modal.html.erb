<p><a class="btn" href="#ideaModal" data-toggle="modal">I have an idea</a></p>

<div class="modal fade" id="ideaModal"> 
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal">×</button>
    <h3>Yeah!  Tell us about your awesome idea!</h3>
  </div>
  <%= form_for(@idea) do |f| %>
  <div class="modal-body">
    <% if @idea.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@idea.errors.count, "error") %> prohibited this idea from being saved:</h2>

        <ul>
        <% @idea.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="actions">
      <%= f.label "success_criteria" %>
      <%= f.text_field :success_criteria %>

      <%= f.label "goodwill" %>
      <%= f.text_field :goodwill %>


      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :description %>
      <%= f.text_field :description %>
    </div>
  </div>
  <div class="modal-footer">
    <%= f.submit "Submit for approval", :class => "btn btn-primary" %>
    <a href="#" class="btn" data-dismiss="modal">Cancel</a>
  </div>
  <% end %>
</div>